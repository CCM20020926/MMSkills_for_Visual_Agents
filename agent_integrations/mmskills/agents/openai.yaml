interface:
  display_name: "MMSkills"
  short_description: "Search and load multimodal skill packages for visual-agent tasks"
  default_prompt: "Use $mmskills to find relevant MMSkills packages, download them if needed, and read their SKILL.md, runtime states, and visual references for the current GUI-agent task."
